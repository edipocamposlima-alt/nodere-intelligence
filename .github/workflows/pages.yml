name: Legacy GitHub Pages disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled legacy channel
        run: |
          echo "GitHub Pages is not the official NODERE deployment channel."
          echo "Use Vercel project 'web' with Root Directory apps/web."
